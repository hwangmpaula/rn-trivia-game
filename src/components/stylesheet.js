import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    button : {
        alignSelf : 'stretch',
    },
    buttonText : {
        color : 'white'
    },
    wrapper : {
        flex : 1,
        width : '100%',
        height : '100%',
    }
})

export default styles
